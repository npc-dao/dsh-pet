# 安全策略

## 支持范围

安全修复以默认分支的最新代码为准。预发布阶段不承诺旧版本的补丁支持。

## 报告漏洞

请不要为未公开漏洞创建公开 Issue。通过 GitHub 仓库的 **Security → Report a vulnerability** 提交私密报告；如果该入口不可用，请联系仓库维护者并仅提供最小复现信息。

报告应包含受影响版本、攻击前提、可复现步骤、预期影响，以及已知的缓解方式。请勿在未经许可的系统上测试。

## 重要边界

宠物目录和图集属于 Host 本机数据。相关 HTTP 路由只接受 DSH Connection 判定为回环可信的请求，并校验 Host、Origin 与 Fetch Metadata。发现安全绕过时，请同时说明浏览器访问地址与请求头。
